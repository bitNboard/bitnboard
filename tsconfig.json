// tsconfig.json
{
    "compilerOptions": {
      "baseUrl": ".",                // Required
      "paths": {
        "@/*": ["src/*"]             // ✅ Alias `@` to your `src` folder
      }
    }
  }
  